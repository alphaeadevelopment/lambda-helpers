{
  "name": "alphaeadev-lambda-utils",
  "version": "1.0.3",
  "description": "Lambda helper functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cross-env NODE_ENV=production webpack",
    "deploy": "npm run build && npm run deploy:version",
    "deploy:version": "npm version patch && npm publish"
  },
  "author": "Graham King <gking2224@gmail.com>",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "cross-env": "^5.1.3",
    "webpack": "^3.10.0"
  },
  "dependencies": {
    "lodash": "^4.17.5"
  },
  "peerDependencies": {
    "lodash": "^4.17.5"
  }
}
